// Solo lo usa Jest (babel-jest). Vite usa esbuild y no lee este archivo.
module.exports = {
  presets: [
    ['@babel/preset-env', { targets: { node: 'current' } }],
    ['@babel/preset-react', { runtime: 'automatic' }],
  ],
};
